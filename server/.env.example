# Server Configuration
PORT=5000
JWT_SECRET=your_jwt_secret_key_here

# Optional: MongoDB connection string (falls back to local JSON database if not provided)
MONGODB_URI=

# Optional: Google Gemini API Key for real-time generative AI responses (AIzaSy...)
GEMINI_API_KEY=
